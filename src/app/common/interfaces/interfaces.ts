export interface CourseContent {
  title: string,
  duration: number,
  date: number,
  description: string,
  topRated?: boolean,
  id: number
}
export interface LocalStorage {
  "login": string,
  "password": string
}
